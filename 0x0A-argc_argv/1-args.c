#include <stdio.h>
/**
 * main - entry point
 * @argc: count for args
 * @argv: array of char
 * Return: always 0
 */

int main(int argc, char *argv[])
{
	 if (!*argv)
	 return (-1)
	 printf("%d\n", argc -1);
	 return (0);
}
